# Placeholder encrypted secret scaffold.
# Replace after configuring age keys in .sops.yaml:
#   sops secrets/rke2-token.enc.yaml
#
# Expected plaintext shape:
#   rke2-token: <shared-cluster-token>
#
# Wire in a host module:
#   sops.defaultSopsFile = ../secrets/rke2-token.enc.yaml;
#   sops.secrets.rke2-token = { };
#   rke2nixos.server.tokenFile = config.sops.secrets.rke2-token.path;
#
# IMPORTANT: generate the token once at first bootstrap and reuse forever.
# Regenerating breaks joining servers/agents and node rebuilds.

rke2-token: REPLACE_WITH_SOPS_ENCRYPT_LAB_TOKEN
